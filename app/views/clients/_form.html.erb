<%= form_with(model: client) do |form| %>
  <% if client.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(client.errors.count, "error") %> prohibited this client from being saved:</h2>

      <ul>
        <% client.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

        <div class= "row  justify-content-evenly ">
            <%# <div class="card col-12 col-sm-5 col-md-3 col-lg-3  "  %>
          <div class="card col-12 col-sm-8 col-md-6  "  
                style="border:2px solid Tomato; background-color:rgb(208, 211, 212);">
              <div class="card-body ">
                    <h5 class="card-title"> clientes <%= @client.id %> </h5>
                    <div>
                          <div>
                            <%= form.label :name, style: "display: block" %>
                            <%= form.text_field :name %>
                          </div>
                          <div>
                            <%= form.label :age, style: "display: block" %>
                            <%= form.text_field :age %>
                          </div>
                  <div class = "d-flex justify-content-end mt-3 gap-3"> 
                    <%= form.submit class: "btn btn-success px-5 py-3  fw-lith " %>
                    <%= link_to "volver cliente", clients_path, class: "btn btn-info  px-5 py-2  fw-lith " %>
                <div>
            <div>
          <div>
        </div>
  </div>
<% end %>
