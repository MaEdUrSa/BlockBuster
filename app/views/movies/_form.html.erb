<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
    <div id="movies"  class = "container mt-3  mb-5  text-primary ">
      <div class= "row  justify-content-evenly ">
            <div class="card col-12 col-sm-8 col-md-6  "  
                style="border:2px solid Tomato; background-color:rgb(208, 211, 212);">
              <div class="card-body ">

                <div>
                  <%= form.label :Pëlicula, style: "display: block" %>
                  <%= form.text_field :name %>
                </div>

                <div>
                    <%= form.label :Cliente, style: "display: block" %>
                    <%= form.collection_select :client_id, @clients, :id, :name, { prompt: "Selecciona un Cliente" }  %>
                </div>

                <div class = "d-flex justify-content-end mt-3 gap-3"> 
                    <%= link_to "Retonar Pelicula", movies_path, class: "btn btn-info  px-3 py-2  fw-lith "  %>
                    <%= form.submit class: "btn btn-success px-2 py-2 fw-lith " %>
                </div>
              </div>
        
            </div>  
      </div>
    </div>
<% end %>
